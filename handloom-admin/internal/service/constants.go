package service

// Shared structured-log / metric-label keys.
const keyProductID = "product_id"

// Common metric-label fallback values, de-duplicated across the package.
const (
	labelUnknown = "unknown"
	labelOther   = "other"
)
