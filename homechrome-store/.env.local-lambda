# Local dev — LocalStack Lambda backend on :4566
# Usage: npm run dev:lambda
NEXT_PUBLIC_API_URL=http://localhost:4566/restapis/rrtq845uzf/local/_user_request_
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Shared secret with backend for cache revalidation webhook
# REVALIDATE_SECRET=

# CloudFront distribution ID for edge cache invalidation on revalidate calls
# CLOUDFRONT_DIST_ID=
